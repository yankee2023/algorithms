/**
 * @brief ナップザック問題
*/

#include <bits/stdc++.h>
using namespace std;

int main()
{
int N, W;
cin >> N >> W;
int weight[N], value[N];
for (int i = 0; i < N; i++) {
    cin >> weight[i] >> value[i];
}

}